<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:wmbXamarin"
             x:Class="wmbXamarin.MainPage">

    <!--<ScrollView>
        <StackLayout
            HorizontalOptions="Center"
            VerticalOptions="CenterAndExpand">
            <Label Text="Id do usuário" TextColor="Black" FontSize="Small"/>
            <Label Text="{Binding UserId}" TextColor="Gray" FontSize="Medium"/>
            <Label Text="Token do usuario" TextColor="Black" FontSize="Small"/>
            <Label Text="{Binding Token}" TextColor="Gray" FontSize="Medium"/>
        </StackLayout>
    </ScrollView>-->

    <StackLayout
        HorizontalOptions="Center"
        VerticalOptions="CenterAndExpand">
        <Button Text="Logar com face"
                BackgroundColor="Blue"
                FontSize="Medium"
                TextColor="White"
                x:Name="LoginButton"></Button>

        <Label TextColor="Black"
               FontSize="Medium"
               x:Name="InfoLabel"></Label>
        
    </StackLayout>
</ContentPage>